# Plumstead Pantry — Website Design C

Single-page redesign for **Plumstead Pantry** restaurant. Design variant C.

Built with a self-contained design-canvas runtime — no build step, no dependencies.  
Open `index.html` in any browser or deploy directly to Vercel.

## Deploy
Static site — push to GitHub and connect to Vercel (Framework: Other).
